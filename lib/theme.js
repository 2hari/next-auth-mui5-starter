import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#202020',
    },
    secondary: {
      main: '#A06E5F',
    },
    background: {
      default: '#EFE9E2',
      paper: '#ffffff',
    },
  },
});

export default theme